window.onload = () => {
    // Traditional function notation of the same function on line 7
    // function sum( firstParam, secondParam ) {
    //     return firstParam + secondParam;
    // }

    let sum = (firstParam, secondParam) => firstParam + secondParam;

    alert(sum(3,4));
};
