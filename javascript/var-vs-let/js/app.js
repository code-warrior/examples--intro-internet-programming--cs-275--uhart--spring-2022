window.onload = () => {
    let message = document.getElementById(`msg`);
    message.textContent += y;
    let x = 10; // Rarely NOT use
    var y = 20; // Rarely use
    console.log(x);
    message.textContent += x;
};
